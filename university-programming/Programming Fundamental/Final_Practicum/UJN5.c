#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main(){
	char cmd[10];
	int saldo, biaya, n;
	scanf("%d", &saldo);
	scanf(" %d", &n);
	for (int i=0; i<n; i++){
		scanf(" %s %d", cmd, &biaya);
		
	}
	
	scanf("%s", a);
	if (strcmp(a, "Yes")==0){
		printf("***\n");
	}else{
		printf("###\n");
	}
}