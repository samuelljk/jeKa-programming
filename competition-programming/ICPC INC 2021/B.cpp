#include <bits/stdc++.h>

using namespace std;

int checker(char s[500]){
	for (int i=0; i<strlen(s); i++){
		
	}
}

int main(){
	char s[500];
	cin >> s;
	if (strlen(s)==2){
		cout << s[0] << " " << s[1];
	}	
}